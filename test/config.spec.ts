import {ConfigComponent} from '../src/components/config'

describe('The config component', () => {
	it('Should be a valid config object', ()=>{
		//Free test until we get pathing for the config working.
		expect(1).toBeTruthy(); 
	});
});
