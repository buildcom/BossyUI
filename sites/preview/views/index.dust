<!DOCTYPE html>
<html>
<head lang="en">
    <meta charset="UTF-8">
    <title>BossyUI Preview</title>
    <link href="lib/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="css/preview.css" rel="stylesheet">
    <link href="dist/css/bossy.css" rel="stylesheet">
</head>
<body ng-app="PreviewApp">
    <nav class="navbar navbar-default navbar-fixed-top" ng-controller="NavCtrl">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand" href="#">
                    BossyUI Preview
                </a>
            </div>
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" ng-click="toggleDirectives()">Directives <span class="caret"></span></a>
                    <ul class="dropdown-menu" ng-class="{show: view.directives}" ng-click="toggleDirectives()">
                        <li ng-repeat="directive in directives" class="directive-list"><a href="#{{ directive }}">{{ directive }}</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </nav>
    <div class="container" ng-controller="TabsCtrl">
        <div class="row">
            <div class="col-xs-12">
                <h3>Preview <span style="text-transform: capitalize" ng-show="view.name">for: {~lb}{~lb}name{~rb}{~rb}</span></h3>
                <br/>
                <div>
                    <ul class="nav nav-tabs" role="tablist">
                        <li role="presentation" ng-class="{active: tabs.directive}" ng-click="toggleTabs()"><a aria-controls="directive" role="tab" data-toggle="tab">Directive</a></li>
                        <li role="presentation" ng-class="{active: tabs.options}" ng-click="toggleTabs()"><a aria-controls="options" role="tab" data-toggle="tab">Options</a></li>
                    </ul>
                    <div class="tab-content">
                        <div role="tabpanel" class="tab-pane" ng-class="{active: tabs.directive}" id="home">
                            <br/>
                            <div ui-view="directive"></div>
                        </div>
                        <div role="tabpanel" class="tab-pane" ng-class="{active: tabs.options}" id="profile"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    {LRScript|s}
    <script src="lib/angular/angular.min.js"></script>
    <script src="lib/ui-router/release/angular-ui-router.min.js"></script>
    <script src="dist/js/bossy.all.js"></script>
    <script src="js/app.js"></script>
</body>
</html>